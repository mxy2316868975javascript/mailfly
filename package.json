{
  "name": "mailfly",
  "version": "2.0.0",
  "description": "Cloudflare Workers Temp Mail",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "postal-mime": "^2.2.0"
  }
}
